(ns kinesis-example-clojure-producer.core-test
  (:require [clojure.test :refer :all]
            [kinesis-example-clojure-producer.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
